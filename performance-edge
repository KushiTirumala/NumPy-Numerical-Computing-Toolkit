# NumPy vs Python lists
import numpy as np
import time

python_list = list(range(1000000))
np_array = np.arange(1000000)

%timeit sum(python_list)      # ~50ms
%timeit np.sum(np_array)      # ~1ms  ← 50x faster!
